{
  "name": "okha-ai-website",
  "version": "1.0.0",
  "description": "Okha.ai website with automated SEO audit functionality using Vercel",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'No build step required for static site'",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "@vercel/kv": "^1.0.1",
    "@vercel/blob": "^0.15.1"
  },
  "devDependencies": {
    "vercel": "^33.0.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}